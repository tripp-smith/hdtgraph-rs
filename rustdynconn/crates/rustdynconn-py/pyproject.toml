[build-system]
requires = ["maturin>=1.4"]
build-backend = "maturin"

[project]
name = "rustdynconn"
version = "0.1.0"
description = "Dynamic connectivity graph (Rust core with Python bindings)."
requires-python = ">=3.9"

[tool.maturin]
python-source = "../../python"
