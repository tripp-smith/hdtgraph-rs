[pytest]
markers =
    core: tests that should run in minimal local environments without optional deps
    networkx: tests that require networkx
    benchmark: benchmark-oriented tests that may require pytest-benchmark
